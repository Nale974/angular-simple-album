// La classe permet de décrire les attributs
// de l'objet `post` passé depuis le parent
export class Post {
    id!: number;
    title!: string;
    description!: string;
    picture!: string;
    likes!: number;
  }